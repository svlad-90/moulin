desc: "Integration test Zephyr build dependencies"
components:
  test:
    build-dir: "workspace"
    default: true
    dependency_policy: build_files
    builder:
      type: zephyr
      board: "native_sim"
      target: app
      work_dir: build
      target_images:
        - "build/zephyr/zephyr.bin"
